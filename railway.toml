[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma db push && npx tsx prisma/seed.ts && next start -p $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
